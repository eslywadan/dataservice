version: '3.9'
services:
  web:
    build: .
    ports:
      - "8080:8080"
  dataapi-cache:
    image: "redis:alpine"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - ~DockerRun/dataapi/redis-data:\data